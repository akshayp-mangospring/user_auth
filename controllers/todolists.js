const Todolist = require('../models/Todolist');

const createTodoList = async (r, w) => { };

const getAllTodoLists = async (r, w) => { };

const editTodoList = (r, w) => { };

const deleteTodoList = (r, w) => { };

module.exports = {
  getAllTodoLists,
  createTodoList,
  editTodoList,
  deleteTodoList
};
